# Archived code

Components removed from the live app but kept for possible future restore.
Files here are **not** part of the build — this folder sits outside `src/`,
so nothing here is compiled, bundled, linted, or formatted.

- `BuildSummaryBar.tsx` — the Build tab "Hero Glance" summary card,
  removed 2026-06-19. Restore instructions are in the file's header comment.
